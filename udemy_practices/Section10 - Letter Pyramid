#include <iostream>
#include <string>

using namespace std;

int main() {
    string s{"C++isFun!"};

    int n = s.length();

    for(int i=0; i < n;  i++)
    {
        for(int j=0; j < n - i -1; j++)
        {
            cout << " "; 
        }
        
        for(int j = 0; j <= i; j++)
        {
            cout << s.at(j); 
        }
        
        for(int j = i-1; j >=0; j--)
        {
            cout << s.at(j);
        }
        
        cout << endl;
    }

    return 0;
}
